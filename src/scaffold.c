#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "ioHandler.h"

int main(int args, char **argv){
	
	//Var
	char* in;

	int i = 5;
	while(i-- > 0){
		
		//in = softenStringInput();
		//free(in);
		
	}
		
	return 0;
	
}


/*

 while(strcmp((in = softenStringInput()), "q") != 0)
 if(strcmp(in, "s") == 0)
 while(strcmp((in = softenStringInput()), "q") != 0);
 else if(strcmp(in, "i") == 0)
 while(softenIntInput() != -1);

*/
